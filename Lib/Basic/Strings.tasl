(/ Strings Module Description:
/ Contains functions for working with strings in memory.
/ Strings are lists with characters in them.
)

/ Replace all upercase characters with lowercase characters.
#Strings.ToLowercase

/ Replace all lowercase characters with uppercase characters.
#Strings.ToUppercase

/ Compact a string by using 2 characters per word instead of just 1.
#Strings.Pack

/ Change a packed string to an unpacked, normal string.
#Strings.Unpack
